<h1>ブログ一覧</h1>

<table border="1">
  <tr>
    <th>タイトル</th>
    <th>内容</th>
    <th colspan="3"></th>
  </tr>
  
  <% @blog.each do |blog| %>
  <tr>
    <td><%= blog.title %></td>
    <td><%= blog.content %></td>
    <td><%= link_to '詳細確認', blog_path(blog.id) %></td>
    <td><%= link_to '編集', edit_blog_path(blog.id) %></td>
    <td><%= link_to '削除', blog_path(blog.id), data:{confirm: '削除していいですか？'},method: :delete %></td>
  </tr>
  <% end %>
</table>


<p><%= link_to '新しくブログを投稿する', new_blog_path %></p>
